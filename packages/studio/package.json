{
  "name": "git3-studio",
  "version": "0.1.0",
  "description": "Localhost Studio GUI for git3",
  "license": "MIT",
  "bin": {
    "git3": "./dist/bin.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node scripts/clean.mjs && tsup && node scripts/copy-ui.mjs",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "git3": "workspace:*",
    "express": "^4.21.2",
    "multer": "^2.0.0",
    "open": "^10.1.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/multer": "^1.4.12"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
